extends layout

block content
	h1= title
	h2 Upload a photo
	form(action='/photos', method='post', enctype='multipart/form-data')
		label Photo:
		input(type='file', name='photo')
		br
		input(type='submit')
